#include <stdio.h> //引入標頭檔<stdio.h> 以便標準輸入輸出

int main(){ 	//main() 作為一隻程式的進入點，讓他知道從這裡開始執行

	//行6: print為印出之意 而f則為format格式 該行即印出學號後換行
	printf("My student ID is 40771131H\n"); 	

	//行9: 印出名字後換行
	printf("My Name is Yu-Chen Lin\n");

	/*
	行16: 一個函式需要給
	  (1)輸入值 (2)做什麼事 (3)輸出 
	此作為main()函數的輸出值，告訴編譯器可以跳出了
	*/
	return 0;
}
